{
  "unlock_code_input": {
    "type": "string",
    "description": "解锁码输入",
    "hint": "输入解锁码后保存配置，再由管理员发送解锁指令。解锁后会自动清空",
    "default": ""
  },
  "sleep_commands": {
    "type": "list",
    "description": "睡觉指令列表",
    "hint": "触发睡觉的指令",
    "items": { "type": "string" },
    "default": ["睡觉", "sleep"]
  },
  "wake_commands": {
    "type": "list",
    "description": "起床指令列表",
    "hint": "触发起床的指令",
    "items": { "type": "string" },
    "default": ["起床", "醒来", "wake"]
  },
  "unlock_command": {
    "type": "string",
    "description": "解锁指令",
    "hint": "敏感锁定后的解锁指令",
    "default": "解锁"
  },
  "enable_force_unlock": {
    "type": "bool",
    "description": "启用强制解锁",
    "hint": "启用后，管理员可发送「强制解锁」直接解锁（兜底机制）",
    "default": true
  },
  "locked_reply_cooldown": {
    "type": "int",
    "description": "锁定提醒冷却时间（秒）",
    "hint": "锁定状态下发送提醒的冷却时间，设为0则每次都提醒",
    "default": 30
  },
  "require_prefix": {
    "type": "bool",
    "description": "是否需要前缀触发",
    "hint": "启用后，指令需要加上配置的唤醒前缀才能触发",
    "default": false
  },
  "sleep_require_admin": {
    "type": "bool",
    "description": "睡觉是否需要管理员权限",
    "hint": "启用后，只有管理员才能触发睡觉指令",
    "default": false
  },
  "wake_require_admin": {
    "type": "bool",
    "description": "起床是否需要管理员权限",
    "hint": "启用后，只有管理员才能触发起床指令",
    "default": false
  },
  "default_duration": {
    "type": "int",
    "description": "默认睡觉时长（秒）",
    "hint": "不指定时长时的默认睡觉时间，范围 60-86400 秒",
    "default": 600
  },
  "max_duration_command": {
    "type": "int",
    "description": "指令触发最大时长（秒）",
    "hint": "通过指令触发的睡觉最大时长，默认12小时(43200秒)，范围 60-86400 秒",
    "default": 43200
  },
  "max_duration_auto": {
    "type": "int",
    "description": "自判定休眠最大时长（秒）",
    "hint": "LLM自判定休眠的最大时长，默认3小时(10800秒)，范围 60-86400 秒",
    "default": 10800
  },
  "sleep_reply": {
    "type": "string",
    "description": "睡觉回复语",
    "hint": "支持变量: {duration}(时长秒), {expiry_time}(到期时间)",
    "default": "好的，我去睡觉了~💤"
  },
  "wake_reply": {
    "type": "string",
    "description": "起床回复语",
    "hint": "支持变量: {duration}(已睡觉时长秒), {expiry_time}(当前时间)",
    "default": "早安~我醒来了☀️"
  },
  "llm_tool_enabled": {
    "type": "bool",
    "description": "启用 LLM 工具",
    "hint": "允许 LLM 调用睡觉工具",
    "default": true
  },
  "lock_secret": {
    "type": "string",
    "description": "敏感锁定密钥",
    "hint": "用于生成解锁码的密钥，请修改为随机字符串以提高安全性",
    "default": "astrbot_sleep_secret"
  },
  "clear_lock_on_startup": {
    "type": "bool",
    "description": "启动时清空锁定记录",
    "hint": "启用后，AstrBot 重启时会自动清空所有敏感锁定记录（兜底机制）",
    "default": true
  },
  "unlock_code_interval": {
    "type": "int",
    "description": "解锁码有效期（秒）",
    "hint": "解锁码的有效时间，超过此时间后解锁码会变化。设为0则解锁码不会变化",
    "default": 60
  },
  "lock_reply_template": {
    "type": "text",
    "description": "锁定时的提示模板",
    "hint": "触发锁定时的提示。支持变量: {reason}, {lock_time}, {unlock_code}, {unlock_command}, {group_id}, {unlock_code_interval}",
    "default": "🔒 当前群已被锁定\n原因: {reason}\n锁定时间: {lock_time}\n\n解锁码: {unlock_code}\n（有效期 {unlock_code_interval} 秒）\n\n请在后台配置文件中输入解锁码并保存后，由管理员发送「{unlock_command}」指令解锁。"
  },
  "locked_reply_template": {
    "type": "text",
    "description": "已锁定状态的提示模板",
    "hint": "锁定后发送消息时的提示。支持变量: {reason}, {lock_time}, {unlock_code}, {unlock_command}, {group_id}, {unlock_code_interval}",
    "default": "🔒 当前群已被锁定\n原因: {reason}\n\n请在后台配置文件中输入正确的解锁码后，由管理员发送解锁指令\n（解锁码每 {unlock_code_interval} 秒变化一次）"
  },
  "scheduled_sleep_enabled": {
    "type": "bool",
    "description": "启用定时睡觉",
    "hint": "在指定时间段自动睡觉",
    "default": false
  },
  "scheduled_sleep_times": {
    "type": "text",
    "description": "定时睡觉时间段",
    "hint": "每行一个时间段，格式: HH:MM-HH:MM，如 23:00-07:00 表示晚上11点到早上7点",
    "default": "23:00-07:00"
  },
  "spam_detect_enabled": {
    "type": "bool",
    "description": "启用刷屏检测",
    "hint": "启用群消息计数功能，用于刷屏检测和自动起床判断",
    "default": false
  },
  "spam_threshold": {
    "type": "int",
    "description": "刷屏判定阈值（消息数）",
    "hint": "在检测窗口内超过此消息数则判定为刷屏",
    "default": 10
  },
  "spam_window": {
    "type": "int",
    "description": "刷屏检测窗口（秒）",
    "hint": "消息计数的时间窗口，默认60秒",
    "default": 60
  },
  "spam_auto_sleep_duration": {
    "type": "int",
    "description": "刷屏自动睡觉时长（秒）",
    "hint": "检测到刷屏后自动睡觉的时长，默认30分钟(1800秒)",
    "default": 1800
  },
  "group_card_update_enabled": {
    "type": "bool",
    "description": "启用群昵称更新",
    "hint": "睡觉时更新群昵称显示剩余时间（仅支持 QQ）",
    "default": false
  },
  "group_card_template": {
    "type": "string",
    "description": "群昵称模板（指令触发）",
    "hint": "指令触发的睡觉使用的模板。支持变量: {original_name}, {original_card}, {original_nickname}, {remaining}(剩余时间), {remaining_seconds}(剩余秒数)",
    "default": "{original_name}[睡觉中 {remaining}]"
  },
  "group_card_template_auto": {
    "type": "string",
    "description": "群昵称模板（自判定休眠）",
    "hint": "LLM自判定休眠时使用的模板。支持变量同上",
    "default": "{original_name}[静默中 {remaining}]"
  },
  "group_card_template_locked": {
    "type": "string",
    "description": "群昵称模板（敏感锁定）",
    "hint": "敏感锁定时使用的模板。支持变量: {original_name}, {original_card}, {original_nickname}",
    "default": "{original_name}[已锁定]"
  },
  "priority": {
    "type": "int",
    "description": "插件优先级",
    "hint": "事件处理优先级，数值越大优先级越高",
    "default": 10000
  }
}
